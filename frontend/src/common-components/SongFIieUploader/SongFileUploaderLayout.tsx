import {LayoutProps} from "@/types/LayoutProps";

const SongFileUploaderLayout = (props: LayoutProps) => {
    const {children} = props;

    return (
        <div>
            {children}
        </div>
    )
}

export default SongFileUploaderLayout;