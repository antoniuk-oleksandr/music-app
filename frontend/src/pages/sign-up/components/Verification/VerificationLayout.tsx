import {LayoutProps} from "@/types/LayoutProps";

const VerificationLayout = (props: LayoutProps) => {
    const {children} = props;

    return (
        <div>
            {children}
        </div>
    )
}

export default VerificationLayout;