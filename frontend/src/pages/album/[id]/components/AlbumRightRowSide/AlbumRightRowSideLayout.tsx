import {LayoutProps} from "@/types/LayoutProps";

const AlbumRightRowSideLayout = (props: LayoutProps) => {
    const {children} = props;

    return (
        <div>
            {children}
        </div>
    )
}

export default AlbumRightRowSideLayout;