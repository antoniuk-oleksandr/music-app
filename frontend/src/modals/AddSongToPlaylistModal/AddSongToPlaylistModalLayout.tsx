import {LayoutProps} from "@/types/LayoutProps";

const AddSongToPlaylistModalLayout = (props: LayoutProps) => {
    const {children} = props;

    return (
        <div>
            {children}
        </div>
    )
}

export default AddSongToPlaylistModalLayout;